# deps

This directory contains external libraries and tool dependencies for the project.

Its contents are automatically populated by CMake using the **FetchContent** feature, based on the configuration defined in **CMakeDeps.cmake**.
